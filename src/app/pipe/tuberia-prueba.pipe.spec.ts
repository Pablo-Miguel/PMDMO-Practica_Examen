import { TuberiaPruebaPipe } from './tuberia-prueba.pipe';

describe('TuberiaPruebaPipe', () => {
  it('create an instance', () => {
    const pipe = new TuberiaPruebaPipe();
    expect(pipe).toBeTruthy();
  });
});
