import { ClickUpperDirective } from './click-upper.directive';

describe('ClickUpperDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickUpperDirective();
    expect(directive).toBeTruthy();
  });
});
